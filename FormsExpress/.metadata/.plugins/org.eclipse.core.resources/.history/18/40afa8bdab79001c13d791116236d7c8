package com.formsExpress.Repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

@Repository
public interface UserRepository extends JpaRepository<List, Integer>{
	
	
	@Query(value = "Select * from User where Customer_Id = :cid", nativeQuery = true)
	public List getAllUsers(@Param("cid") int Customer_Id);
}
